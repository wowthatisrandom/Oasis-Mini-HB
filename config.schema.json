{
  "pluginAlias": "OasisMini",
  "pluginType": "platform",
  "singular": true,
  "headerDisplay": "Homebridge plugin for Oasis Mini kinetic sand table. Control your sand table via Siri and HomeKit.",
  "footerDisplay": "For help, see the [README](https://github.com/wowthatisrandom/Oasis-Mini-HB#readme).",
  "schema": {
    "type": "object",
    "properties": {
      "name": {
        "title": "Name",
        "type": "string",
        "default": "Oasis Mini",
        "description": "The name that will appear in HomeKit for this device."
      },
      "serial": {
        "title": "Device Serial Number",
        "type": "string",
        "description": "The serial number of your Oasis Mini device. Found on the bottom of the device or in the Oasis app under Settings. For multiple tables, use the Tables list below instead.",
        "placeholder": "OM000000000"
      },
      "tables": {
        "title": "Tables (for multiple devices)",
        "type": "array",
        "description": "Configure multiple tables on the same Oasis account. If set, each entry gets its own set of HomeKit accessories.",
        "items": {
          "type": "object",
          "properties": {
            "serial": {
              "title": "Serial Number",
              "type": "string",
              "placeholder": "OM000000000"
            },
            "name": {
              "title": "Name",
              "type": "string",
              "description": "Display name for this table in HomeKit (optional)."
            }
          },
          "required": ["serial"]
        }
      },
      "email": {
        "title": "Oasis Account Email",
        "type": "string",
        "format": "email",
        "description": "The email for your Oasis account — the same login you use in the official Oasis app. The Oasis cloud now requires per-account sign-in."
      },
      "password": {
        "title": "Oasis Account Password",
        "type": "string",
        "description": "Your Oasis account password. Stored only in your local Homebridge config and sent only to the official Oasis cloud (app.grounded.so) to sign in."
      },
      "pollingInterval": {
        "title": "Polling Interval (seconds)",
        "type": "integer",
        "default": 30,
        "minimum": 5,
        "maximum": 300,
        "description": "How often to poll the device for status updates (backup to real-time MQTT)."
      }
    },
    "required": ["email", "password"]
  },
  "form": null,
  "display": null
}
